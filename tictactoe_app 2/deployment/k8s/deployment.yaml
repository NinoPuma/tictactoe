apiVersion: apps/v1
kind: Deployment
metadata:
  name: mi-app
  namespace: despliegue-k8s-proyecto-nspc

spec:
  replicas: 1
  selector:
    matchLabels:
      app: mi-app
  template:
    metadata:
      labels:
        app: mi-app
    spec:
      containers:
      - name: mi-app
        image: img-proyecto-prueba3  
        imagePullPolicy: Never
        ports:
        - containerPort: 3000  
